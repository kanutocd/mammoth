global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: mammoth
    metrics_path: /metrics
    static_configs:
      - targets:
          - mammoth-observability:9393
